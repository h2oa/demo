name: Test

on:
  pull_request:
    paths:
      - requirements.txt
        
jobs:
  changes:
    name: "changes"
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "${{github.head_ref}}"
          echo "ok"
